// TODO: learning rate, learning rate decay (function?), training split vs. cross-validation convergence criteria (epochs, loss, delta performance, etc.), learning function (gradient descent, momentum, etc.) cost function?
pub trait Trainer<T> {	// TODO: make into a struct?
	// TODO: fn train(
}
